diff '--color=auto' -urN o/olefy.py n/olefy.py
--- o/olefy.py	2026-06-10 17:15:19.090339230 +0200
+++ n/olefy.py	2026-06-10 17:17:43.888050717 +0200
@@ -58,7 +58,7 @@
 if not olefy_listen_addr_string:
     olefy_listen_addr = ""
 else:
-    addr_re = re.compile('[\[" \]]')
+    addr_re = re.compile('[]" []')
     olefy_listen_addr = addr_re.sub('', olefy_listen_addr_string.replace("'", "")).split(',')
 
 # log runtime variables
@@ -206,7 +206,14 @@
 
 # start the listeners
 async def main():
-    loop = asyncio.get_running_loop()
+    if sys.version_info < (3, 10):
+        loop = asyncio.get_event_loop()
+    else:
+        try:
+            loop = asyncio.get_running_loop()
+        except RuntimeError:
+            loop = asyncio.new_event_loop()
+        asyncio.set_event_loop(loop)
     server = await loop.create_server(AIO, olefy_listen_addr, olefy_listen_port)
     for sockets in server.sockets:
         logger.info('serving on {}'.format(sockets.getsockname()))
